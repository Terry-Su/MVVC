const state = {

}

const mutation = {
  updateState(state, param) {

  }
}

const actions = {
  UPDATE_STATE(context, param) {
    context.commit('updateState', param)
  }
}

const getters = {

}

module.exports = {
  state,
  mutation,
  actions,
  getters
}